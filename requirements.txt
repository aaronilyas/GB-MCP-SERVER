# Host / local development (`pip install -r requirements.txt`, pytest).
# Image builds do not use this file:
#   requirements-server.txt   -> Dockerfile.server (gb-mcp-server)
#   requirements-instance.txt -> Dockerfile.instance (gb-pyboy-instance)
-r requirements-server.txt
-r requirements-instance.txt
iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
